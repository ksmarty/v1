## What

<!-- One or two sentences. -->

## Checks

- [ ] `go vet ./... && go test ./... && go build ./...` pass
- [ ] `cd web && npm run build` passes (typecheck)
- [ ] Conventional commit message (`feat:` / `fix:` / …) — it drives the release bump
- [ ] Docs updated if commands, layout, or config keys changed (README.md / AGENTS.md)
